\begin{table}[t]
\centering
\caption{Validation chain. Each row is checked against the one above it, so the end-to-end accuracy numbers rest on circuit simulation rather than on a noise model.}
\label{tab:validation}
\begin{tabular}{lll}
\toprule
Level & Checked against & Worst disagreement \\
\midrule
ngspice DC solve & --- (reference) & --- \\
Nodal model & ngspice, 87,168 tiles & 3.41e-07\% \\
Vectorised solver & nodal model & $<10^{-12}$ (rel.) \\
End-to-end inference & vectorised solver & exact (same code path) \\
\bottomrule
\end{tabular}
\end{table}
